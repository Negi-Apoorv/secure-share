Set-up .env:
  there is a file "envexample.txt" open it
  copy its content and paste in newly created .env file
  generate file encryption key
  update sql password
  update secret key

How to run:
  naigate to main branch "secure-share"
  run app.py by python app.py
  click or visit local host  http://127.0.0.1:5000

Features:
  it shares file as its goal
  encryption and decryption works
  user can download, open file

fututre goals:
  password impimentation
  notification 
  rights management
